export enum EProtectedPage {
	DASHBOARD = "dashboard",
	ROOM = "rooms",
	TENANT = "tenants",
	BILLABLES = "billables",
	REPORTS = "reports",
	SETTINGS = "settings",
	TRANSACTIONS = "transactions",
}

export enum EPublicPage {
	LOGIN = "login",
}

export enum ERoomType {
	SMALL = "Small",
	MEDIUM = "Medium",
	LARGE = "Large",
}
