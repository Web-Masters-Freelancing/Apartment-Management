import { Box, Typography } from "@mui/material";
import WrapperLayout from "@/app/wrapper.layout";

const TransactionsPage = () => {
	return (
		<WrapperLayout>
			<Box>
				<Typography variant="h6">Transactions works </Typography>
			</Box>
		</WrapperLayout>
	);
};

export default TransactionsPage;
